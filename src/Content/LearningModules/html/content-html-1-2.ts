export default `
# HTML Structure

### **Understanding HTML Structure**

An HTML document follows a specific structure that helps browsers interpret and display content correctly. It consists of a **DOCTYPE declaration**, followed by nested elements that define the webpage's layout and content.

### **Key Components of HTML Structure:**

- **\`<!DOCTYPE html>\`**: Declares the document type and version of HTML.
- **\`<html>\`**: The root element that contains all other elements.
- **\`<head>\`**: Contains metadata, styles, and links to external resources.
- **\`<body>\`**: Holds the main content displayed on the webpage.

## **Basic HTML Structure Example:**

\`\`\`html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HTML Structure</title>
</head>
<body>
    <h1>Understanding HTML Structure</h1>
    <p>This example shows the essential parts of an HTML document.</p>
</body>
</html>
\`\`\`
`;
